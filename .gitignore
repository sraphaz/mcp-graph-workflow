# Logs
logs/
*.log

# Dependencies
node_modules/

# Build
dist/
build/
skills-graph/
workflow-graph/
CLAUDE.md
# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.claude/rules
.claude/*
# OS
.DS_Store
Thumbs.db
sdk/
# Environment
.env
.env.local
.env.*
raw-*
raw.md
notas.md
docs/raw/
docs/prd/
docs/_internal/prd/
docs/architecture/
docs/reference/
knowledge-export.json
browser-*
!src/core/browser-pilot/
!src/core/browser-pilot/**
!src/mcp/tools/browser-pilot.ts
!src/tests/browser-use-runtime.test.ts
hermes-agent*

# MCP / Local Tools
workflow-graph/
.mcp-graph/
*.db
*.db-shm
*.db-wal

# Serena (local memories and config)
.serena/

# Claude Code local settings
.claude/settings.local.json

# Playwright MCP
.playwright-mcp/

# Dashboard build artifacts
src/web/dashboard/.next/
src/web/dashboard/out/
src/web/dashboard/dist/
src/web/dashboard/node_modules/

# Vendored Copilot Bridge forks — separate workspaces with their own
# package.json + node_modules; tracked elsewhere via the Copilot Bridge
# audit epic (node_26f1f82e064b). Excluded here so the main repo PR
# surface stays slim (236MB combined).
tools/copilot-bridge/
tools/copilot-bridge-cli/

# Local-only baselines / planning notes (per memory rule
# feedback_no_public_design_exposure — design rationale stays local).
.fase-b-baseline.md
baseline-*.png

# Test artifacts
test-results/
test-screenshots/
playwright-report/
coverage/
.nyc_output/
.gitnexus
presentation/public/video/*.mov

# act local CI runner — secrets file is gitignored; the runner itself is
# configured via .actrc (tracked).
.secrets

# Sibling repos accidentally cloned into this tree — never commit
hermes-*/
browser-*-main/

# Real benchmark data + experiment artifacts — design-sensitive, local-only
docs/BENCHMARK-*.md
workflow-graph/h9v2-debug/

# Internal analysis reports + graph exports — design-sensitive
scripts/*-report.json
scripts/speckit-*.json
knowledge-export.json

# Internal research/RAD findings — design rationale stays local
docs/_research/
